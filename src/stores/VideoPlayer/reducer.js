const initialState = {
  videoId: '0ByoQm-vnYw'  
};

const videoPlayer = (state = initialState, action) => {
  switch(action.type) {
    default:
      return state;
  }
}

export default videoPlayer;